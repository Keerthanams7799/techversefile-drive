/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      remotePatterns: [
        {
          hostname: "adorable-squirrel-520.convex.cloud",
        },
      ],
    },
  };

export default nextConfig;
